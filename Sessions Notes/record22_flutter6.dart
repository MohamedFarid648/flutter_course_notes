/*
*1.Flutter life cycle

*1. createState:First Thing
*2. initState: after screen go to tree,called Once(like ngOnInit in angular),(you can call apis here)
*3. didChangeDependencies: when state (dependencies) is changed do some thing,ex: when product is updated do something
*4. build
*5. setState: rebuild widget (do num 3)
*6. didUpdateWidget: after rebuilt widget(compare between old and new values)
*7. deactivate: when widget remove from tree(you can use it again , it still in the context)
*8. dispose: remove widget from the tree finally
*9. reseample: use it in debug (hot reload) 
 */

/*
 *2.
 *VerticalDivider (line vertical).
 *3.ExpandWidget(flex:1),ExpandWidget(flex:2)
 
 */
