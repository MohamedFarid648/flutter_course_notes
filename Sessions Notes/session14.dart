/*

1.Timer.periodic(duration , func); //like setInterval in js
func will be called every duration(ex:2 sec) 

2. Controller Stream is a midddle ware between Sync and stream
take value from sync and return stream

4.StreamTransformer :Has a namedConstructor called fromHandlers(
handleData: Do something when receiving data
handleError: Do something if you add sinc.addError
handleDone: Do something after finishing
)

handleData(type,sink) => this sink is new one
3.Stream<int> x = mystream.asBroadCastStream():
x.listen(event=>...)//do something on the value
x.listen(event=>...)//do another thing on the value


5.Dart isolates
 */
/*
6.http package
dio package
 */
